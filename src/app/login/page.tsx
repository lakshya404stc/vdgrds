import React from 'react'
import { ProfileForm } from '../external-components/Login'

type Props = {}

export default function Login({}: Props) {
  return (
    <div>
        <ProfileForm/>
    </div>
  )
}