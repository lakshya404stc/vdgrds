import React from 'react'
import { ProfileForm } from '../../components/register'

type Props = {}

export default function Login({}: Props) {
  return (
    <div>
        <ProfileForm/>
    </div>
  )
}